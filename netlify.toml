[build]
  publish = "dist"
  command = "npm run build"
  functions = "netlify/functions"

[dev]
  command = "npm run dev:local"
  framework = "#auto"
  targetPort = 3001  # Point to this project's Vite server (different from other projects)

[context.dev.environment]
  BYPASS_AUTH_LOCALHOST = "true"

[build.environment]
  NODE_VERSION = "22"

[[headers]]
  for = "/*"
  [headers.values]
    Cross-Origin-Opener-Policy = "same-origin-allow-popups"
    Cross-Origin-Embedder-Policy = "unsafe-none"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Disable SPA redirect in dev mode - Vite handles it
[context.dev]
  [[context.dev.redirects]]
    from = "/.netlify/*"
    to = "/.netlify/:splat"
    status = 200

# SPA fallback for production only
[context.production]
  [[context.production.redirects]]
    from = "/*"
    to = "/index.html"
    status = 200
    force = false


# Environment variables needed:
# MONGODB_URI - MongoDB connection string
# MONGODB_DB_NAME - Database name (default: bi-forecast)
# JWT_SECRET - Secret key for JWT tokens
# ENCRYPTION_KEY - Secret key for encrypting OAuth tokens
# GOOGLE_CLIENT_ID - Google OAuth client ID
# QBO_CLIENT_ID - QuickBooks OAuth client ID
# QBO_CLIENT_SECRET - QuickBooks OAuth client secret
# QBO_REDIRECT_URI - QuickBooks OAuth redirect URI